# OpenGrep 全量扫描：手动触发，审计历史 backlog，不阻塞 PR。
# 与 opengrep-precise.yml 共用 opengrep-rules.yml 和 run-opengrep.sh，区别仅扫描范围（全量 vs diff）。
name: OpenGrep — Full

on:
  workflow_dispatch:

concurrency:
  group: opengrep-full-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  security-events: write

jobs:
  scan:
    name: Scan full repository
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: 安装 opengrep
        env:
          # 见 opengrep-precise.yml 的说明：直接拉发布产物 + sha256 固定，避开 api.github.com 限流。
          OPENGREP_VERSION: v1.22.0
          OPENGREP_SHA256: 45bcd58440e397ed52c50e953ccf5948909ea77087c9186fc7d277216f62e319
        run: |
          dest="$HOME/.opengrep/cli/${OPENGREP_VERSION}"
          mkdir -p "$dest"
          curl -fsSL --retry 5 --retry-delay 5 --retry-connrefused \
            -o "$dest/opengrep" \
            "https://github.com/opengrep/opengrep/releases/download/${OPENGREP_VERSION}/opengrep_manylinux_x86"
          echo "${OPENGREP_SHA256}  $dest/opengrep" | sha256sum -c -
          chmod +x "$dest/opengrep"
          ln -sfn "$dest" "$HOME/.opengrep/cli/latest"
          echo "$HOME/.opengrep/cli/latest" >> "$GITHUB_PATH"

      - run: opengrep --version

      - name: 全量扫描
        run: |
          mkdir -p .opengrep-out
          bash .ci/run-opengrep.sh --sarif --error

      - name: 上传 SARIF 到 Code Scanning
        if: always() && hashFiles('.opengrep-out/precise.sarif') != ''
        continue-on-error: true # 未开 Code Scanning 时上传会失败；扫描结论由前一步 --error 决定，上传不阻塞
        uses: github/codeql-action/upload-sarif@411bbbe57033eedfc1a82d68c01345aa96c737d7 # v4
        with:
          sarif_file: .opengrep-out/precise.sarif
          category: opengrep-full
