# Miloco Backend 地址（默认本地 1810 端口）
MILOCO_BASE_URL=http://127.0.0.1:1810

# Bearer Token（留空则自动从 ~/.openclaw/miloco/config.json 读取）
MILOCO_TOKEN=

# 请求超时（秒）
MILOCO_TIMEOUT=30

# 是否验证 TLS（Miloco 默认 HTTP，保持 false）
MILOCO_TLS_VERIFY=false
